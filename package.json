{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "axios": "^1.6.7"
  }
}
