{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "bcryptjs": "^3.0.2"
  }
}
